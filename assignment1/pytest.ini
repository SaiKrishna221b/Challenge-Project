[pytest]
# Pytest configuration file
markers =
    timeout: marks tests as timeout tests (deselect with '-m "not timeout"')

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    -v
    --strict-markers
    --tb=short

